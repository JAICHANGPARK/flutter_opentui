# Select

세로 리스트 선택 UI입니다.

## Construct

```dart
final select = Select(
  id: 'action',
  options: const <String>['Build', 'Test', 'Run'],
  selectedIndex: 0,
);
```

## 기본 키 처리

- `ArrowUp`, `ArrowDown`
