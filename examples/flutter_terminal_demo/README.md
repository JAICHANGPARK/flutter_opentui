# flutter_terminal_demo

Flutter example using `opentui_flutter`.

## Run

```bash
cd /Users/jaichang/Documents/GitHub/flutter_opentui
dart run melos bootstrap
cd examples/flutter_terminal_demo
flutter run
```

The demo supports both hardware keyboard input and touch controls for mobile.
